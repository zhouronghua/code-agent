---
description: 代码修改前必须通过 benchmark 门禁检查，确保 agent 质量指标不退化。每次修改后运行 npm run benchmark:check 确认通过再推送。
alwaysApply: true
---

# Benchmark Quality Gate (基准测试门禁)

每次修改代码后，在 push 之前必须执行 benchmark 检查，确保 agent 整体质量指标不退化。

## 检查命令

```bash
# 快速检查（推荐，每次改完代码后执行）
npm run benchmark:check

# 严格模式（回归则阻止 push）
BENCHMARK_STRICT=1 npm run benchmark:check

# 跳过检查
BENCHMARK_DISABLE=1 npm run benchmark:check

# 查看完整报告
npm run benchmark

# 查看当前基准文档
cat docs/benchmark.md
```

## 门禁阈值（6 项核心指标 — v3 宽松版）

由于 LLM 输出天然具有不确定性，v3 大幅放宽阈值并默认采用 warn-only 模式。

| 指标 | 方向 | 容忍度 | 说明 |
|------|------|--------|------|
| `successRate` | ↑ 越高越好 | 允许降 8% | 任务完成率 |
| `avgDurationSec` | ↓ 越低越好 | 允许增 150% | 平均任务耗时 |
| `avgSteps` | ↓ 越低越好 | 允许增 100% | 平均步骤数 |
| `avgToolCalls` | ↓ 越低越好 | 允许增 100% | 平均工具调用数 |
| `toolExecSuccessRate` | ↑ 越高越好 | 允许降 10% | 工具执行成功率 |
| `avgToolCallsPerStep` | ↓ 越低越好 | 允许增 50% | 每步平均调用数 |

## 环境变量

| 变量 | 说明 | 默认值 |
|------|------|--------|
| `BENCHMARK_RUNS` | 每个任务重复运行次数取中位数 | 3 |
| `BENCHMARK_STRICT` | 回归时 exit 1 阻止 push | 未设置（warn only） |
| `BENCHMARK_DISABLE` | 完全跳过 benchmark 检查 | 未设置 |

## 判断标准

- ✅ **全部通过** → 可以 push，Git pre-push hook 也会自动通过
- ⚠️ **回归但非 strict 模式** → 警告但不阻止 push（默认行为）
- ❌ **BENCHMARK_STRICT=1 且回归** → 阻止 push，排查退化原因
- ⚠️ 如果退化是**有意为之**（如新增功能必然增加步骤数）→ `npm run benchmark:save` 更新基线
- 🚨 紧急情况 → `BENCHMARK_DISABLE=1` 或 `git push --no-verify` 跳过检查

## 工作流

```
修改代码 → npm run build → npm run benchmark:check
                                   │
                          ✅ 通过 → git push
                          ⚠️ 警告 → git push（默认非阻塞）
                          ❌ 阻止 → 排查修复 → 重新检查（仅 strict 模式）
                                   │
                          npm run benchmark:save (更新基线)
```

## 注意事项

- 默认使用 3 次运行取中位数来平滑 LLM 非确定性
- `~/.codeagent/tasks/` 会自动积累新的任务日志作为 benchmark 数据源
- 建议每次重要版本发布前手动 `npm run benchmark:save` 更新基线
- 详细数据见 `docs/benchmark.md`

# Publish target: GitHub Packages (npm registry for this repository).
# The token is never stored here — export NODE_AUTH_TOKEN before publishing:
#   NODE_AUTH_TOKEN=<your PAT with write:packages> npm publish
@zhouronghua:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
